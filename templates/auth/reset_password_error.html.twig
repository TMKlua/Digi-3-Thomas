{% extends 'base.html.twig' %}

{% block title %}Digi-3 - Erreur de réinitialisation{% endblock %}

{% block body %}
<main id="main-content" role="main">
  <div class="auth-container">
    <div class="auth-section flex">
      <div class="error-container">
        <h2>Erreur de réinitialisation</h2>
        
        <div class="error-message">
          {% if error %}
            <p>{{ error }}</p>
          {% else %}
            <p>Une erreur est survenue lors de la réinitialisation de votre mot de passe.</p>
          {% endif %}
        </div>
        
        <div class="error-actions">
          <a href="{{ path('app_auth') }}" class="auth-link">Retour à la page de connexion</a>
          <p>Si vous avez besoin d'aide, veuillez contacter l'administrateur.</p>
        </div>
      </div>
    </div>
  </div>
</main>
{% endblock %} 