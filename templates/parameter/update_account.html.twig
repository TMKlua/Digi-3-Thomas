{% extends 'base.html.twig' %}

{% block body %}
    <h1>Update Account</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Update Account</button>
    {{ form_end(form) }}

    <a href="{{ path('app_parameter_generaux') }}">Cancel</a>
{% endblock %}
